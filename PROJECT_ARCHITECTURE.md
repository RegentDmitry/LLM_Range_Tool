# RangeLab AI - Техническая архитектура

```
╔═══════════════════════════════════════╗
║                                       ║
║            RANGELAB AI                ║
║         ─────────────────             ║
║    AI-Powered Range Analysis          ║
║    Advanced Research Tool             ║
║                                       ║
╚═══════════════════════════════════════╝
```

**Домен:** rangelab.ai
**GitHub:** github.com/RegentDmitry/rangelab-ai
**Tagline:** "Master Your Poker Strategy with AI"

---

## 📋 Содержание

1. [Обзор проекта](#обзор-проекта)
2. [MVP: Два сценария](#mvp-два-сценария)
3. [Архитектура системы](#архитектура-системы)
4. [База данных](#база-данных)
5. [Монетизация](#монетизация)
6. [Технологический стек](#технологический-стек)
7. [Структура проектов](#структура-проектов)
8. [Пошаговый план](#пошаговый-план)
9. [Регистрации и настройки](#регистрации-и-настройки)
10. [Планы развития](#планы-развития-post-mvp)

---

## 🎯 Обзор проекта

### Цель

Создать веб-сервис для анализа покерных диапазонов с использованием:
- Деревьев решений (Decision Trees)
- Системы бакетирования (85 признаков для Омахи)
- LLM-анализа через OpenAI API
- Системы подписок и кредитов

### Философия

**Пользователь предоставляет данные → Выбирает сценарий → Получает интеллектуальный LLM-отчёт**

Сервис работает по модели SaaS с подписками и кредитами.

### Целевая аудитория

- Покерные игроки (Омаха PLO4/PLO5)
- Игроки в Холдем (Texas Hold'em) — future
- Студенты покерных школ
- Аналитики покерных данных
- Создатели покерного контента

---

## 🎨 MVP: Два сценария

### Сценарий 1: Анализ одного диапазона 📊

**Что делает:**
Пользователь загружает один CSV с диапазоном, система строит дерево решений и генерирует LLM-отчёт.

**User Flow:**
```
1. Загрузка:
   - CSV файл (bet_range.csv)
   - Борд (9s6d5c)

2. Обработка:
   - Бакетирование 49,746 комбо (85 фич)
   - Построение дерева решений
   - Feature importance
   - Генерация Mermaid диаграммы

3. Результат:
   - Статистика (точность, глубина, листья)
   - LLM отчёт: "Этот диапазон агрессивный на 65%..."
   - Интерактивное дерево решений
   - Follow-up чат для уточнений
```

---

### Сценарий 2: Lead vs NoLead (сравнение диапазонов) ⚖️

**Что делает:**
Пользователь загружает два набора диапазонов (Lead и NoLead), система сравнивает стратегии.

**Контекст:**
- **Lead:** Villain мог сделать donk bet, но чекнул (voluntary check)
- **NoLead:** Villain не мог сделать donk bet (forced check)

**User Flow:**
```
1. Загрузка:
   Lead сценарий:
   - bet_range.csv
   - check_range.csv

   NoLead сценарий:
   - nolead_bet_range.csv
   - nolead_check_range.csv

   Борд: 9s6d5c

2. Обработка:
   - Бакетирование всех 4 файлов
   - Построение двух деревьев (Lead, NoLead)
   - Сравнительный анализ
   - Генерация двух Mermaid диаграмм

3. Результат:
   - Сравнительная таблица статистики
   - Feature importance для обоих
   - LLM отчёт: "Lead стратегия на 34% агрессивнее..."
   - Ключевые различия (straight_blocker vs pair)
   - Side-by-side визуализация деревьев
   - Follow-up чат
```

**Пример LLM отчёта:**
```
На борде 9s6d5c Lead стратегия значительно агрессивнее NoLead (+34.2%).

Ключевые находки:

Lead (voluntary check):
• straight_blocker - самая важная фича (37.3%)
• no_draw важен для блокер-блефов (12.5%)
• Можно блефовать с чистыми блокерами (node_id 33: 98.7% bet)

NoLead (forced check):
• straight_blocker2 - самая важная фича (46.4%)
• pair важнее для шоудауна (10.4%)
• Нужен реальный эквити, нельзя переблефовать (node_id 6: 99.8% check)

Вывод: Информация о том, что оппонент мог, но не стал донк-бетить,
радикально меняет стратегию.
```

---

## 🏗️ Архитектура системы

### Общая схема

```
┌─────────────────────────────────────────────────────────────────┐
│                         ПОЛЬЗОВАТЕЛЬ                             │
│  1. Регистрация (Clerk) → Начисление стартовых кредитов         │
│  2. Выбор сценария (Single/Lead vs NoLead)                      │
│  3. Загрузка CSV файлов                                          │
│  4. Проверка кредитов → Анализ → LLM отчёт                      │
└────────────────────────────┬────────────────────────────────────┘
                             │ HTTPS
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    VERCEL (Frontend CDN)                         │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │              Next.js 14 + React 18 App                     │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │  @clerk/nextjs (Auth UI)                             │ │ │
│  │  │  • Sign Up / Sign In                                 │ │ │
│  │  │  • User Profile                                      │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │  Главная: Выбор сценария (2 карточки)               │ │ │
│  │  │  [1] Анализ одного диапазона                         │ │ │
│  │  │  [2] Lead vs NoLead                                  │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌─────────────────┐ │ │
│  │  │ File Upload  │  │ Credits View │  │  Billing Page   │ │ │
│  │  │ (Drag&Drop)  │  │  (Balance)   │  │  (Stripe UI)    │ │ │
│  │  └──────────────┘  └──────────────┘  └─────────────────┘ │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │    Progress Bar (WebSocket real-time)                │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │  Results Dashboard:                                   │ │ │
│  │  │  • LLM Report (markdown)                             │ │ │
│  │  │  • Mermaid Tree Viewer (interactive)                 │ │ │
│  │  │  • Feature Importance Table                          │ │ │
│  │  │  • Follow-up Chat                                    │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  └────────────────────────────────────────────────────────────┘ │
└────────────────────────────┬────────────────────────────────────┘
                             │ REST API + Bearer Token
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                   RAILWAY (Backend Platform)                     │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                   FastAPI Backend                          │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │  Auth Middleware: verify_clerk_token()               │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │  API Endpoints:                                       │ │ │
│  │  │  • GET  /api/scenarios        (список сценариев)    │ │ │
│  │  │  • POST /api/analyze          (запуск анализа)      │ │ │
│  │  │  • POST /api/chat             (follow-up LLM)       │ │ │
│  │  │  • GET  /api/credits          (баланс кредитов)     │ │ │
│  │  │  • POST /api/payments         (покупка кредитов)    │ │ │
│  │  │  • POST /webhooks/stripe      (Stripe events)       │ │ │
│  │  │  • POST /webhooks/clerk       (User sync)           │ │ │
│  │  │  • WebSocket /ws/{session_id} (прогресс)            │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │  Сценарии (Analysis Pipelines):                      │ │ │
│  │  │  1. SingleRangeScenario                              │ │ │
│  │  │     - Загрузка 1 CSV                                 │ │ │
│  │  │     - Бакетирование → Дерево → LLM отчёт             │ │ │
│  │  │  2. LeadVsNoLeadScenario                             │ │ │
│  │  │     - Загрузка 4 CSV (Lead bet/check, NoLead bet/check) │ │ │
│  │  │     - Два дерева → Сравнение → LLM отчёт             │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │  Services:                                            │ │ │
│  │  │  • CreditsService (check/deduct/add)                 │ │ │
│  │  │  • StripeService (payments/subscriptions)            │ │ │
│  │  │  • LLMService (OpenAI reports/chat)                  │ │ │
│  │  │  • TreeBuilderService (scikit-learn)                 │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │  Core Logic (ваш существующий код):                  │ │ │
│  │  │  • lib/buckets.py       (85 признаков Омахи)        │ │ │
│  │  │  • lib/card.py          (карты и комбинации)        │ │ │
│  │  │  • build_bucket_matrix.py (матрицы)                 │ │ │
│  │  │  • build_combined_tree.py (деревья)                 │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌────────────────────┐  ┌────────────────────┐                │
│  │   PostgreSQL       │  │      Redis         │                │
│  │  (Railway)         │  │  (Railway)         │                │
│  │  • users           │  │  • Sessions        │                │
│  │  • credits         │  │  • Job queue       │                │
│  │  • subscriptions   │  │  • Rate limiting   │                │
│  │  • usage_logs      │  │                    │                │
│  │  • payments        │  │                    │                │
│  └────────────────────┘  └────────────────────┘                │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Внешние сервисы                               │
│  ┌──────────────┐  ┌──────────────┐  ┌────────────────────┐    │
│  │  Clerk       │  │  Stripe      │  │  OpenAI API        │    │
│  │  (Auth)      │  │  (Payments)  │  │  (GPT-4o-mini)     │    │
│  │  Бесплатно   │  │  % от платежа│  │  Usage-based       │    │
│  └──────────────┘  └──────────────┘  └────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🗄️ База данных

### PostgreSQL (Railway)

```sql
-- Пользователи
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    email VARCHAR(255) UNIQUE NOT NULL,
    name VARCHAR(255),
    avatar_url TEXT,
    clerk_user_id VARCHAR(255) UNIQUE NOT NULL,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- Кредиты
CREATE TABLE credits (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id) ON DELETE CASCADE UNIQUE,
    amount INTEGER NOT NULL DEFAULT 0,
    total_earned INTEGER NOT NULL DEFAULT 0,
    total_spent INTEGER NOT NULL DEFAULT 0,
    updated_at TIMESTAMP DEFAULT NOW()
);

-- Подписки
CREATE TABLE subscriptions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id) ON DELETE CASCADE UNIQUE,
    plan VARCHAR(50) NOT NULL DEFAULT 'free',  -- 'free', 'starter', 'pro'
    status VARCHAR(50) NOT NULL DEFAULT 'active',
    stripe_subscription_id VARCHAR(255),
    stripe_customer_id VARCHAR(255),
    current_period_start TIMESTAMP,
    current_period_end TIMESTAMP,
    cancel_at_period_end BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

-- История использования
CREATE TABLE usage_logs (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id) ON DELETE CASCADE,
    scenario VARCHAR(100) NOT NULL,  -- 'single_range', 'lead_vs_nolead'
    cost INTEGER NOT NULL,
    status VARCHAR(50) NOT NULL,  -- 'success', 'failed', 'in_progress'
    metadata JSONB,  -- {board, file_sizes, processing_time}
    created_at TIMESTAMP DEFAULT NOW()
);

-- Транзакции кредитов
CREATE TABLE credit_transactions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id) ON DELETE CASCADE,
    amount INTEGER NOT NULL,  -- + начисление, - списание
    type VARCHAR(50) NOT NULL,  -- 'subscription', 'purchase', 'usage'
    description TEXT,
    reference_id VARCHAR(255),
    created_at TIMESTAMP DEFAULT NOW()
);

-- Платежи
CREATE TABLE payments (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id) ON DELETE CASCADE,
    amount INTEGER NOT NULL,  -- В центах
    currency VARCHAR(3) DEFAULT 'usd',
    status VARCHAR(50) NOT NULL,
    stripe_payment_intent_id VARCHAR(255),
    credits_granted INTEGER,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Индексы
CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_users_clerk_id ON users(clerk_user_id);
CREATE INDEX idx_usage_logs_user_id ON usage_logs(user_id);
CREATE INDEX idx_usage_logs_created_at ON usage_logs(created_at);
```

---

## 💰 Монетизация

> **Примечание:** Описанные способы монетизации являются возможными вариантами. Не все из них обязательно будут реализованы в MVP.

### Модель оплаты

Система работает на основе кредитов с несколькими тарифными планами:

| План | Уровень | Кредиты | Описание |
|------|---------|---------|----------|
| **Free** | Бесплатно | Малое количество | Для тестирования (несколько анализов) |
| **Starter** | Низкая стоимость | Среднее количество | Для регулярного использования |
| **Pro** | Средняя стоимость | Большое количество | Для профессионалов |

### Стоимость операций (в кредитах)

```python
# Сценарий 1: Single range
SINGLE_RANGE_BASE = "low"      # Базовая стоимость
SINGLE_RANGE_LARGE = "medium"  # Для больших файлов

# Сценарий 2: Lead vs NoLead (4 файла)
LEAD_VS_NOLEAD_BASE = "medium"     # Базовая стоимость
LEAD_VS_NOLEAD_LARGE = "high"      # Для больших файлов

# Follow-up чат
CHAT_MESSAGE = "very_low"  # За каждое LLM сообщение
```

### Pay-as-you-go

Дополнительные кредиты можно покупать разовыми пакетами. Большие пакеты получают бонусные кредиты.

### Способы оплаты

**Фиатные платежи (основной):**
- Stripe (кредитные карты, Google Pay, Apple Pay)
- Поддержка подписок и разовых платежей

**Криптовалютные платежи (опционально):**
- **Coinbase Commerce** — популярное и удобное решение для приёма криптовалют
  - Поддержка: Bitcoin (BTC), Ethereum (ETH), USDC, USDT, Litecoin (LTC)
  - Автоконвертация в фиат опционально
  - Webhooks для интеграции с системой кредитов
  - Низкие комиссии (1% за транзакцию)
  - Без KYC для базового использования

**Альтернативы для крипто:**
- BTCPay Server (self-hosted, без комиссий)
- NOWPayments (более 200 криптовалют)
- Triple-A (enterprise-ready)

---

## 🛠️ Технологический стек

### Frontend (Vercel)

| Технология | Версия | Назначение |
|-----------|--------|------------|
| **Next.js** | 14.x | React framework (App Router) |
| **React** | 18.x | UI библиотека |
| **TypeScript** | 5.x | Типобезопасность |
| **@clerk/nextjs** | ^4.27 | Аутентификация |
| **@stripe/stripe-js** | ^2.2 | Stripe клиент |
| **@stripe/react-stripe-js** | ^2.4 | Stripe компоненты |
| **@tanstack/react-query** | 5.x | API кэширование |
| **Tailwind CSS** | 3.x | Стилизация |
| **shadcn/ui** | Latest | UI компоненты |
| **Mermaid.js** | 10.x | Визуализация деревьев |
| **Recharts** | 2.x | Графики |
| **Zustand** | 4.x | State management |

```json
// frontend/package.json
{
  "dependencies": {
    "next": "14.2.0",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "@clerk/nextjs": "^4.27.0",
    "@stripe/stripe-js": "^2.2.0",
    "@stripe/react-stripe-js": "^2.4.0",
    "@tanstack/react-query": "^5.0.0",
    "tailwindcss": "^3.4.0",
    "mermaid": "^10.6.0",
    "recharts": "^2.10.0",
    "zustand": "^4.4.0"
  }
}
```

---

### Backend (Railway)

| Технология | Версия | Назначение |
|-----------|--------|------------|
| **FastAPI** | 0.104+ | REST API framework |
| **Python** | 3.11+ | Основной язык |
| **Uvicorn** | 0.24+ | ASGI сервер |
| **SQLAlchemy** | 2.x | ORM для PostgreSQL |
| **Alembic** | 1.x | Миграции БД |
| **asyncpg** | 0.29+ | Async PostgreSQL драйвер |
| **Pydantic** | 2.x | Валидация данных |
| **Stripe** | Latest | Платёжная система |
| **httpx** | 0.25+ | HTTP клиент (Clerk API) |
| **Redis** | 5.x | Кэш и очереди |

```txt
# backend/requirements.txt
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Database
sqlalchemy>=2.0.0
alembic>=1.12.0
asyncpg>=0.29.0

# Auth & Payments
stripe>=7.0.0
httpx>=0.25.0

# Data validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# ML/Data Science (ваш код)
pandas>=2.0.0
scikit-learn>=1.3.0
numpy>=1.24.0
tqdm>=4.65.0

# Redis
redis>=5.0.0

# Utils
python-dotenv>=1.0.0
python-multipart>=0.0.6
```

---

### Системы бакетирования

**Омаха (PLO4/PLO5):**
- Ваша система из 85 признаков
- Уже реализована в `lib/buckets.py`
- Готова к использованию в MVP

**Холдем (Texas Hold'em):**
- Система бакетирования как в PokerTracker 4
- Будет реализована в будущем (Post-MVP)

---

### Инфраструктура

| Сервис | Назначение | Категория стоимости |
|--------|-----------|---------------------|
| **Vercel** | Frontend hosting | Бесплатно (Hobby tier) |
| **Railway** | Backend compute + PostgreSQL + Redis | Низкая стоимость |
| **Clerk** | Аутентификация | Бесплатно (generous free tier) |
| **Stripe** | Платёжная система | % от транзакций |
| **OpenAI** | LLM API | Usage-based (зависит от объёма) |
| **GitHub** | Git репозиторий | Бесплатно |

---

## 📁 Структура проектов (монорепо)

```
poker-analyzer/                     # GitHub репозиторий
├── .github/
│   └── workflows/
│       └── deploy.yml              # CI/CD (опционально)
│
├── frontend/                       # Next.js → Vercel
│   ├── src/
│   │   ├── app/                    # Next.js 14 App Router
│   │   │   ├── layout.tsx
│   │   │   ├── page.tsx            # Главная: выбор сценария
│   │   │   ├── scenarios/
│   │   │   │   ├── single/
│   │   │   │   │   └── page.tsx    # Сценарий 1
│   │   │   │   └── comparison/
│   │   │   │       └── page.tsx    # Сценарий 2 (Lead vs NoLead)
│   │   │   ├── billing/
│   │   │   │   └── page.tsx        # Stripe billing
│   │   │   └── results/
│   │   │       └── [id]/
│   │   │           └── page.tsx    # Результаты анализа
│   │   │
│   │   ├── components/
│   │   │   ├── ui/                 # shadcn компоненты
│   │   │   ├── ScenarioCard.tsx    # Карточка сценария
│   │   │   ├── FileUpload.tsx      # Drag & drop
│   │   │   ├── CreditsDisplay.tsx  # Баланс кредитов
│   │   │   ├── TreeViewer.tsx      # Mermaid визуализация
│   │   │   ├── LLMReport.tsx       # Отчёт от LLM
│   │   │   ├── ChatFollowUp.tsx    # Чат для уточнений
│   │   │   └── ProgressBar.tsx     # WebSocket прогресс
│   │   │
│   │   ├── lib/
│   │   │   ├── api-client.ts       # API клиент для Railway
│   │   │   ├── types.ts            # TypeScript типы
│   │   │   └── utils.ts
│   │   │
│   │   └── hooks/
│   │       ├── useAnalysis.ts      # React Query
│   │       ├── useCredits.ts
│   │       └── useWebSocket.ts
│   │
│   ├── public/
│   ├── package.json
│   ├── tsconfig.json
│   ├── tailwind.config.ts
│   ├── next.config.js
│   └── vercel.json
│
├── backend/                        # FastAPI → Railway
│   ├── app/
│   │   ├── main.py                 # FastAPI app
│   │   │
│   │   ├── api/
│   │   │   ├── __init__.py
│   │   │   ├── scenarios.py        # GET /api/scenarios
│   │   │   ├── analyze.py          # POST /api/analyze
│   │   │   ├── chat.py             # POST /api/chat
│   │   │   ├── credits.py          # GET /api/credits
│   │   │   ├── payments.py         # POST /api/payments
│   │   │   ├── webhooks.py         # POST /webhooks/{stripe,clerk}
│   │   │   └── websocket.py        # WebSocket /ws/{session_id}
│   │   │
│   │   ├── scenarios/              # Сценарии анализа
│   │   │   ├── __init__.py
│   │   │   ├── base.py             # BaseScenario
│   │   │   ├── single_range.py     # Сценарий 1
│   │   │   └── lead_vs_nolead.py   # Сценарий 2
│   │   │
│   │   ├── models/                 # SQLAlchemy модели
│   │   │   ├── __init__.py
│   │   │   ├── user.py
│   │   │   ├── credits.py
│   │   │   ├── subscription.py
│   │   │   ├── usage_log.py
│   │   │   └── payment.py
│   │   │
│   │   ├── schemas/                # Pydantic схемы
│   │   │   ├── __init__.py
│   │   │   ├── scenario.py
│   │   │   ├── analyze.py
│   │   │   ├── chat.py
│   │   │   └── response.py
│   │   │
│   │   ├── services/
│   │   │   ├── credits.py          # CreditsService
│   │   │   ├── stripe.py           # StripeService
│   │   │   ├── llm.py              # LLMService (OpenAI)
│   │   │   ├── tree_builder.py     # TreeBuilderService
│   │   │   └── file_processor.py   # CSV обработка
│   │   │
│   │   └── core/
│   │       ├── config.py           # Settings (Pydantic)
│   │       ├── database.py         # PostgreSQL connection
│   │       ├── redis.py            # Redis connection
│   │       └── auth.py             # Clerk middleware
│   │
│   ├── lib/                        # ВАШ КОД (без изменений!)
│   │   ├── __init__.py
│   │   ├── buckets.py              # 85 признаков
│   │   ├── card.py                 # Карты
│   │   ├── build_bucket_matrix.py  # Матрицы
│   │   └── build_combined_tree.py  # Деревья
│   │
│   ├── alembic/                    # Миграции БД
│   │   ├── versions/
│   │   ├── env.py
│   │   └── script.py.mako
│   │
│   ├── tests/
│   │   ├── test_api.py
│   │   ├── test_scenarios.py
│   │   └── test_credits.py
│   │
│   ├── requirements.txt
│   ├── Procfile                    # Railway
│   ├── railway.json                # Railway config
│   ├── alembic.ini
│   └── pytest.ini
│
├── docs/                           # Документация
│   └── API.md
│
├── .env.example
├── .gitignore
├── README.md
└── PROJECT_ARCHITECTURE.md         # Этот файл
```

---

## 🚀 Пошаговый план разработки

> **Философия:** Сначала создаём работающий продукт (auth + сценарии + результаты), затем добавляем монетизацию.

### Фаза 0: Подготовка

**Задачи:**
- [ ] Создать монорепо структуру
- [ ] Настроить Git репозиторий на GitHub
- [ ] Зарегистрироваться на платформах:
  - [ ] Vercel
  - [ ] Railway
  - [ ] Clerk
  - [ ] OpenAI
  - [ ] (Stripe — на этапе монетизации)
- [ ] Настроить .env файлы

---

### Фаза 1: Backend Core (без монетизации)

#### FastAPI + Database (минимальная БД)
- [ ] Создать FastAPI проект
- [ ] Настроить PostgreSQL на Railway
- [ ] SQLAlchemy модели — **только User** (без Credits, Subscriptions)
- [ ] Alembic миграции (первая миграция)
- [ ] Базовые endpoints (health, root)

#### Auth
- [ ] Clerk middleware для FastAPI
- [ ] Webhook для синхронизации users (`POST /webhooks/clerk`)
- [ ] Endpoint `GET /api/user` (профиль пользователя)
- [ ] Тесты

#### Scenarios System
- [ ] Базовый класс `BaseScenario`
- [ ] `SingleRangeScenario` (интеграция с вашим кодом)
- [ ] `LeadVsNoLeadScenario` (4 файла)
- [ ] Endpoint `POST /api/analyze` (БЕЗ проверки кредитов)
- [ ] Endpoint `GET /api/scenarios` (список доступных сценариев)
- [ ] WebSocket для прогресса (`/ws/{session_id}`)

#### Testing & Deploy
- [ ] Unit тесты для всех endpoints
- [ ] Integration тесты
- [ ] Deploy на Railway
- [ ] Проверить работу через curl/Postman

**Результат:** Backend принимает файлы и запускает анализ (deployed)

---

### Фаза 2: LLM Integration

#### OpenAI Setup
- [ ] LLMService с OpenAI SDK
- [ ] Промпты для single range отчёта
- [ ] Промпты для Lead vs NoLead comparison
- [ ] Генерация Mermaid диаграмм

#### Follow-up Chat
- [ ] Endpoint `POST /api/chat`
- [ ] Контекст из предыдущего анализа
- [ ] Streaming ответов (опционально)

#### Testing & Deploy
- [ ] Тесты LLM отчётов (качество ответов)
- [ ] Rate limiting (базовый)
- [ ] Error handling (если OpenAI не отвечает)
- [ ] Deploy обновлений на Railway
- [ ] Проверить качество LLM отчётов

**Результат:** Backend генерирует LLM отчёты (deployed)

---

### Фаза 3: Frontend Core (без billing)

#### Setup + Auth
- [ ] Next.js 14 проект
- [ ] Tailwind CSS + shadcn/ui
- [ ] Clerk integration (@clerk/nextjs)
- [ ] Layout с User Button
- [ ] TanStack Query setup

#### Scenario Selection
- [ ] Главная страница
- [ ] Два ScenarioCard компонента
- [ ] API клиент
- [ ] **БЕЗ Credits display** (пока нет кредитов)

#### File Upload
- [ ] FileUpload компонент (drag & drop)
- [ ] Форма для Single Range (1 CSV)
- [ ] Форма для Lead vs NoLead (4 CSV)
- [ ] Валидация CSV (размер, формат)
- [ ] API запрос к `/api/analyze`

#### Progress + Results
- [ ] WebSocket hook
- [ ] ProgressBar компонент (real-time обновления)
- [ ] Results page
- [ ] LLMReport компонент (markdown рендеринг)
- [ ] Feature Importance таблица

#### Tree Visualization + Chat
- [ ] Mermaid.js integration
- [ ] TreeViewer компонент
- [ ] Interactive nodes (подсветка)
- [ ] Side-by-side для comparison
- [ ] Export PNG/SVG (опционально)
- [ ] ChatFollowUp компонент
- [ ] Mobile responsive
- [ ] Error boundaries

#### Testing & Deploy
- [ ] Playwright E2E тест: регистрация → выбор сценария → загрузка файлов → анализ → LLM отчёт → chat
- [ ] E2E тест: Single Range сценарий
- [ ] E2E тест: Lead vs NoLead сценарий
- [ ] Lighthouse audit (90+ score)
- [ ] Bug fixing
- [ ] Deploy на Vercel
- [ ] Протестировать полный flow в production

**Результат:** Полный working MVP БЕЗ монетизации (deployed, можно использовать)

---

### Фаза 4: Добавление Монетизации

> **Важно:** Только после того как core flow полностью работает!

#### Backend: Credits System
- [ ] Добавить модели: Credits, Subscriptions, UsageLog, Payments
- [ ] Alembic миграция для новых таблиц
- [ ] CreditsService (check/deduct/add)
- [ ] Обновить `POST /api/analyze` — добавить проверку и списание кредитов
- [ ] Endpoint `GET /api/credits` (баланс пользователя)

#### Stripe Integration
- [ ] Зарегистрироваться на Stripe
- [ ] StripeService (создание платежей)
- [ ] Endpoint `POST /api/payments` (Payment Intent)
- [ ] Настроить Products & Prices в Stripe (Free, Starter, Pro)
- [ ] Webhook `POST /webhooks/stripe` (начисление кредитов после оплаты)

#### Subscriptions
- [ ] Создание подписки в Stripe
- [ ] Обработка recurring payments
- [ ] Cancel/Resume subscription
- [ ] Автоматическое начисление кредитов при продлении

#### Frontend: Billing
- [ ] Credits display (компонент баланса кредитов)
- [ ] Billing page (Stripe Elements)
- [ ] Уведомления о недостатке кредитов
- [ ] История транзакций

#### Testing & Deploy
- [ ] Unit тесты: CreditsService
- [ ] Integration тесты: Stripe webhooks
- [ ] E2E тест: покупка кредитов → списание за анализ
- [ ] E2E тест: подписка → автоматическое начисление
- [ ] E2E тест: недостаточно кредитов → уведомление
- [ ] Deploy Backend (Railway) + Frontend (Vercel)
- [ ] Stripe test mode → live mode
- [ ] Проверить webhooks в production
- [ ] Тестовая покупка реальными деньгами

**Результат:** Полноценный продукт с монетизацией (deployed, ready to launch) 🚀

---

## 🔑 Регистрации и настройки

### 1. GitHub
**URL:** https://github.com
**Действия:**
1. Создать репозиторий `poker-analyzer`
2. Настроить `.gitignore`

---

### 2. Vercel
**URL:** https://vercel.com
**Действия:**
1. Sign up with GitHub
2. Import Project → `poker-analyzer`
3. **Root Directory:** `frontend/`
4. Deploy

**Environment Variables:**
```bash
NEXT_PUBLIC_API_URL=https://your-backend.up.railway.app
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
```

---

### 3. Railway
**URL:** https://railway.app
**Действия:**
1. Sign up with GitHub
2. New Project → Deploy from GitHub
3. **Root Directory:** `backend/`
4. Add PostgreSQL: New → Database → PostgreSQL
5. Add Redis: New → Database → Redis

**Environment Variables:**
```bash
# OpenAI
OPENAI_API_KEY=sk-proj-...

# Clerk
CLERK_SECRET_KEY=sk_live_...
CLERK_PUBLISHABLE_KEY=pk_live_...

# Stripe
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Database (автоматически от Railway)
DATABASE_URL=postgresql://...

# Redis (автоматически от Railway)
REDIS_URL=redis://...

# Frontend (для CORS)
FRONTEND_URL=https://your-app.vercel.app

# App
ENVIRONMENT=production
DEBUG=false
MAX_FILE_SIZE=100  # MB
SESSION_TTL=3600
```

---

### 4. Clerk
**URL:** https://clerk.com
**Действия:**
1. Sign up
2. Create application
3. Configure: Email + Google OAuth
4. Copy API keys
5. Configure webhooks:
   - Endpoint: `https://your-backend.up.railway.app/webhooks/clerk`
   - Events: `user.created`, `user.updated`, `user.deleted`

---

### 5. Stripe
**URL:** https://stripe.com
**Действия:**
1. Sign up
2. Create Products:
   - Starter Plan (подписка)
   - Pro Plan (подписка)
3. Create one-time products для кредитов
4. Configure webhooks:
   - Endpoint: `https://your-backend.up.railway.app/webhooks/stripe`
   - Events: `payment_intent.succeeded`, `invoice.payment_succeeded`
5. Copy API keys

---

### 6. OpenAI
**URL:** https://platform.openai.com
**Действия:**
1. Sign up
2. Create API key
3. Add payment method
4. Set usage limit (рекомендуется для контроля расходов)

---

## 🚀 Планы развития (Post-MVP)

После запуска MVP возможны следующие улучшения и расширения:

### 1. RAG Система для углубленного анализа
- **Описание:** Интеграция Retrieval-Augmented Generation для контекстного анализа
- **Компоненты:**
  - Векторная БД (Pinecone, Weaviate, или ChromaDB)
  - Хранилище документации и стратегий (уже частично реализовано с S3)
  - Embeddings через OpenAI или локальные модели

### 2. Texas Hold'em поддержка
- Система бакетирования аналогичная PokerTracker 4
- Новые сценарии анализа для Hold'em

### 3. Расширенная визуализация
- Визуализация диапазонов
- Экспорт в различные форматы (PDF, PNG, SVG)

---

## 📦 GitHub Repository

### Описание для GitHub:

```markdown
# RangeLab AI

AI-Powered Range Analysis • Advanced Research Tool

Intelligent poker range analysis platform using decision trees
and LLM-generated insights. Built for serious players and
researchers studying poker strategy.

🎯 Features:
• Decision tree visualization with Mermaid.js
• AI-powered strategy analysis via OpenAI
• PLO4/PLO5 support with 85-feature bucketing system
• Lead vs NoLead comparison scenarios
• Interactive follow-up chat with AI

🛠️ Tech Stack:
• Backend: Python, FastAPI, PostgreSQL
• Frontend: Next.js 14, TypeScript, React 18
• AI: OpenAI GPT-4o-mini, LangChain
• Deployment: Railway (backend), Vercel (frontend)

⚠️ Important: RangeLab AI is a POST-GAME analysis tool for learning
and research. It is NOT real-time assistance (RTA) and should never
be used during live play.

📚 Documentation: See PROJECT_ARCHITECTURE.md
```

### GitHub Topics:

```
poker
poker-strategy
decision-tree
machine-learning
ai
llm
openai
fastapi
nextjs
typescript
range-analysis
plo
omaha
poker-ai
analytics
postgresql
react
```

---

## 🎯 Следующие шаги

1. Создать GitHub репозиторий `rangelab-ai`
2. Создать монорепо структуру
3. Зарегистрироваться на платформах (GitHub, Vercel, Railway, Clerk, OpenAI)
4. Начать Фазу 1: Backend Foundation
